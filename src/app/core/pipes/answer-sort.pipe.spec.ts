import { AnswerSortPipe } from './answer-sort.pipe';

describe('AnswerSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AnswerSortPipe();
    expect(pipe).toBeTruthy();
  });
});
